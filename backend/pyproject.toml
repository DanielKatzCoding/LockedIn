[project]
name = "lockedin-backend"
version = "0.1.0"
description = "LockedIn API backend"
requires-python = ">=3.10"
dependencies = [
    "fastapi>=0.115.5",
    "uvicorn[standard]>=0.32.1",
]

[project.optional-dependencies]
dev = []

[tool.uv]
dev-dependencies = []
